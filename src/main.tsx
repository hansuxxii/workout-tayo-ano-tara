import React from "react";
import ReactDOM from "react-dom/client";
import WorkoutTayoAnoTaraApp from "./WorkoutTayoApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <WorkoutTayoAnoTaraApp />
  </React.StrictMode>
);
